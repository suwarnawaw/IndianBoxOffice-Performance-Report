# IndianBoxOffice-Performance-Report
🚀 Project Overview  This project demonstrates an end-to-end modern data analytics pipeline built using Microsoft Fabric, implementing the Medallion Architecture (Bronze → Silver → Gold) to transform raw movie industry data into meaningful business insights.
